
from robel_dclaw_env.custom_service import normalize


# def convexhull_point_scaling(points):
    # '''
    # [mujocoはメートル単位なので[m]に変換したい]
    # input:
    #     points : shape = (N, 2), range = (-1, 1)
    # output:
    #     scaled_points
    # '''



    # points = normalize(normalize)
    # return points
